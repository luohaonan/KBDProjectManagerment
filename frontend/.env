# 开发环境留空：请求发往当前页面的主机（含局域网 IP），由 Vite 代理转发到后端
# 生产构建请设置实际后端地址，例如 VITE_API_BASE_URL=http://your-server:8080
VITE_API_BASE_URL=

# Vite 开发代理目标，可按本机后端启动端口调整，例如 8081
VITE_BACKEND_PROTOCOL=http
VITE_BACKEND_HOST=127.0.0.1
VITE_BACKEND_PORT=8080
